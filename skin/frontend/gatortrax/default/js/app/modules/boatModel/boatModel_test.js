'use strict';

describe('app.boatModel module', function() {

  beforeEach(module('app.boatModel'));

  describe('view1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var boatModelCtrl = $controller('BoatModelCtrl');
      expect(boatModelCtrl).toBeDefined();
    }));

  });
});